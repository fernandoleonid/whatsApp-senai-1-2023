


const buildContacts = () =>{
    const container = document.getElementById('contact')
    const contacts = contacts.map(createContactItem);
    container.append(...contacts)
}

const createContactItem = function(contact){
    
}

buildContacts()